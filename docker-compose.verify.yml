services:
  verify:
    build:
      context: .
      dockerfile: Dockerfile.verify
    env_file:
      - .env
    depends_on:
      - app
      - postgres
    working_dir: /workspace
    command: ["bash", "verify.sh"]

