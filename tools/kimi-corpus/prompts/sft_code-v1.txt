Generate compact lkjai SFT scenario rows.

Return one JSON object only:
{"rows":[...]}

Each row must have exactly these top-level keys:
- messages
- tags
- meta

For each row:
- messages contains one user message only.
- tags includes "kimi_synthetic" and "language:en".
- meta contains the fields below.
- Do not write assistant messages; the local normalizer will build XML actions.
- Use English only.
- Avoid secrets, benchmark text, copyrighted passages, and prompt wrappers.
- Do not mention Kimi, ChatGPT, or this instruction.

Required meta fields:
- template_family: one of direct_finish, read_only_retrieval,
  mutation_confirmation, failure_safety_recovery
- scenario_family_id: unique short slug
- intent: short intent label
- tool_sequence: one of:
  - ["agent.finish"]
  - ["fs.list","agent.finish"]
  - ["fs.read","agent.finish"]
  - ["memory.search","agent.finish"]
  - ["resource.search","agent.finish"]
  - ["resource.fetch","agent.finish"]
  - ["resource.history","agent.finish"]
  - ["resource.preview_markdown","agent.finish"]
  - ["agent.request_confirmation"]
- confirmation_required: true only for ["agent.request_confirmation"]
- grounding_source: "repo_fixture"
- gold_stop_reason: "confirmation_required" for confirmation rows, else "finish"
- fixture_id: one of direct-greeting, workspace-list, workspace-read,
  memory-search, resource-search, resource-fetch, resource-history,
  markdown-preview, create-note, create-media, update-resource, disabled-shell,
  absolute-path, secret-refusal
- source_ref: "repo-grounding-v1"

Batch:
- mode: {{MODE}}
- documents: {{DOCUMENTS}}
- sample: {{SAMPLE_NOTE}}
- domains: {{DOMAIN_PLAN}}

Balance the batch across the four template families. Make user requests
specific, short, and grounded in lkjai workspace/resource tasks.
