You are generating original synthetic assistant-masked SFT examples for lkjai.

OUTPUT STRICT JSONL ONLY. No prose wrapper, no markdown fence, no commentary.
Every line must be exactly one complete JSON object with keys: messages, tags,
meta.

Generate exactly {{DOCUMENTS}} JSONL lines.

Required metadata:
- meta.provenance: "kimi-generated"
- meta.author_type: "external-agent-generated"
- meta.author_model: "kimi-code"
- meta.quality_tier: "high"
- meta.license: "project-local"
- meta.mode: "sft"
- meta.prompt_version: "{{PROMPT_VERSION}}"
- meta.language: "en"
- meta.template_family: one of direct_finish, read_only_retrieval,
  mutation_confirmation, failure_safety_recovery
- meta.scenario_family_id: stable lowercase id for the scenario
- meta.intent: precise intent label
- meta.tool_sequence: exact list of assistant tool names in order
- meta.confirmation_required: boolean
- meta.grounding_source: source label
- meta.gold_stop_reason: "finish" or "confirmation_required"

Global assistant action rules:
- Every assistant message content is exactly one XML <action> block.
- XML has no attributes.
- Include <reasoning>, <tool>, and needed child fields.
- Every completed trace must end with an assistant action whose tool is
  agent.finish.
- `agent.finish` must use `<content>...</content>`. Do not use `<answer>`,
  `<message>`, `<final>`, or `<response>`.
- If a row contains a non-final tool action, include a following tool message
  and a final assistant agent.finish action.
- Do not output a row whose last assistant tool is fs.read, fs.list, shell.exec,
  web.fetch, memory.search, resource.search, resource.fetch, resource.history,
  or resource.preview_markdown.
- For mutation rows, output `agent.request_confirmation` as the last assistant
  action and set gold_stop_reason to confirmation_required.
- `agent.request_confirmation` must include `<summary>`, `<operation>`, and
  `<pending_tool>`.
- Mutation confirmation rows are only for `kjxlkj` resource mutations:
  `resource.create_note`, `resource.create_media`, or `resource.update_resource`.
- Never output resource.create_note, resource.create_media, or
  resource.update_resource unless a prior assistant action in the same row is
  agent.request_confirmation and a user confirmation turn follows.

Template family mix for the batch:
- direct_finish: direct answer, greeting, concise preference, simple correction.
- read_only_retrieval: fs.read, fs.list, memory.search, resource.search,
  resource.fetch, resource.history, or resource.preview_markdown followed by
  tool result and agent.finish.
- mutation_confirmation: agent.request_confirmation for a kjxlkj resource
  mutation only; no mutation execution unless the row includes a later user
  "yes" turn.
- failure_safety_recovery: safe refusal, blocked path, empty results, malformed
  request, or honest tool failure followed by agent.finish.

Content rules:
- English only.
- No copyrighted reproduction, benchmark dumps, web article replicas, or
  memorized material.
- No "as an AI", filler, or generic "Completed task for ..." answers.
- Escape literal <, >, and & inside XML text fields.
- Keep answers compact and useful.

Batch plan:
- {{SAMPLE_NOTE}}
- languages: {{LANGUAGE_PLAN}}
- domains: {{DOMAIN_PLAN}}
- difficulties: {{DIFFICULTY_PLAN}}

Return JSONL now.
