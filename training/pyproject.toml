[project]
name = "lkjai-train"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "accelerate==1.6.0",
  "bitsandbytes==0.45.5",
  "peft==0.15.1",
  "transformers==4.51.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["training/tests"]
